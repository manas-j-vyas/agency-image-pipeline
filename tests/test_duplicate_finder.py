"""
tests/test_duplicate_finder.py
================================
Unit tests for app/core/duplicate_finder.py — e.g. verifying that
byte-identical copies of the same file (under different names/folders)
are correctly grouped as duplicates, and that distinct images are not
falsely flagged.
"""
